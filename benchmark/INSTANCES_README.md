# Instance layout
SQLite files are not stored in git (size). Download via scripts/download_instances.py after the data release is published, or point benchmark/instances at a local copy whose checksums match INSTANCE_CHECKSUMS.json.
