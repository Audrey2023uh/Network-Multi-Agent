\section{Results}
\label{sec:results}

Unless noted, multi-seed means use six frozen instances. Final T1 claims use the architecture-selection anchored head; baselines, deep models, and most ablations use the full-suite harness. Exact floating-point archives are listed in Appendix~\ref{app:supp}.

\subsection{T1 anomaly and T2 failure prediction}
Table~\ref{tab:t1t2} summarizes ranking metrics. The final ECN-v3 anchored detector achieves mean T1 AUPRC $0.115$ (bootstrap 95\% CI $[0.068,0.168]$), above random forest ($0.076$) and the v2 legacy configuration ($0.058$), with mean T1 ROC-AUC $0.753$. Fig.~\ref{fig:baseline} shows the corresponding AUPRC means with 95\% intervals for T1 and T2.

\input{tables/tab_t1t2}

\begin{figure*}[!t]
\centering
\includegraphics[width=0.98\textwidth]{baseline_auprc_mean_ci.pdf}
\caption{Multi-seed mean AUPRC with 95\% CI. (a)~T1 (ECN-v3 = final anchored head). (b)~T2 (ECN column = recommended telem-logistic head).}
\label{fig:baseline}
\end{figure*}

On T2, the recommended head is telemetry logistic regression (mean AUPRC $0.038$), higher than RF ($0.018$). The ECN T2 columns in Table~\ref{tab:t1t2} report the multi-agent proposed head ($0.015$), which is \emph{not} the recommended operating head. We do not claim that multi-agent fusion improves T2 under ultra-rare positives.

Representative ROC and precision--recall curves appear in Fig.~\ref{fig:t1curves}. Discrimination is moderate on ROC, while precision--recall remains challenging for all methods.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.98\textwidth]{T1_v1.1.0-INST_roc_pr.pdf}
\caption{T1 ROC (a) and precision--recall (b) on the primary frozen instance, including the final anchored ECN-v3 head. Legend values are instance-level ROC-AUC / average precision.}
\label{fig:t1curves}
\end{figure*}

Paired Wilcoxon tests and Cliff's $\delta$ (Table~\ref{tab:sig}) show that the T1 mean advantage versus random forest is not significant at $\alpha{=}0.05$ ($p{=}0.688$, $\delta{=}{+}0.389$). Versus logistic and several weaker baselines, paired $p$-values reach $0.031$ (exploratory under multiple comparisons). Stacking versus anchored is not significant ($p{=}0.375$).

\input{tables/tab_significance}

\subsection{Architecture selection and fusion ablation}
Fig.~\ref{fig:archsel} and Table~\ref{tab:ablation} isolate T1 sources of improvement. Leakage-safe feature enrichment accounts for the dominant gain; replacing anchored fusion with stacking on the same enriched features reduces mean AUPRC from $0.115$ to $0.100$. Stacking is therefore a negative ablation.

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{architecture_selection_t1.pdf}
\caption{T1 architecture selection over six seeds (mean AUPRC with 95\% CI).}
\label{fig:archsel}
\end{figure}

\input{tables/tab_ablation}

The T1 \texttt{full} ablation row is a full-suite re-run and may differ slightly from the Table~\ref{tab:t1t2} selection mean.
Table~\ref{tab:modules} and Fig.~\ref{fig:modules} summarize contribution deltas. Feature enrichment versus v2 contributes ${+}0.058$ mean AUPRC. Twin gain under the final head is near zero. Stacking relative to anchored contributes ${-}0.015$. The harness module table reports a slightly negative twin ablation delta (${-}0.004$); both estimates indicate Twin is not the primary T1 ranking driver.

\input{tables/tab_modules}

\begin{figure}[!t]
\centering
\includegraphics[width=0.92\columnwidth]{module_contribution.pdf}
\caption{T1 AUPRC deltas from architecture selection: feature enrichment, twin gain, and stacking versus anchored fusion.}
\label{fig:modules}
\end{figure}

\subsection{Calibration and operating points}
Table~\ref{tab:cal} summarizes calibration. Final T1 raw Brier is $0.151$, worse than RF ($0.023$), motivating optional Beta calibration for probability display without changing AUPRC ranking. Fig.~\ref{fig:t1cal} shows a reliability diagram on v1.1.0-INST.

\input{tables/tab_calibration}

\begin{figure}[!t]
\centering
\includegraphics[width=0.92\columnwidth]{T1_v1.1.0-INST_calibration.pdf}
\caption{T1 reliability diagram on the primary frozen instance for the anchored ECN-v3 head.}
\label{fig:t1cal}
\end{figure}

\subsection{RCA, healing decision support, and cost}
Table~\ref{tab:rcaheal} reports RCA macro-F1, healing scores, and cost. Proposed T3 RCA macro-F1 is $0.254$ (RF+TreeSHAP). Healing with RCA category features reaches macro-F1 $1.000$ on this synthetic setup---an upper bound, not operational perfection; the honest no-category setting yields $0.190$. Fig.~\ref{fig:t3cm} shows a representative confusion matrix. Full-suite evaluation averages about $119.7$\,s per seed.

\input{tables/tab_rca_healing_cost}

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{T3_v1.1.0-INST_cm.pdf}
\caption{Representative T3 RCA confusion matrix (RF+TreeSHAP) on the primary frozen instance.}
\label{fig:t3cm}
\end{figure}

\subsection{Why simplicity remains preferable}
At $\sim$1\% positives, enriched telemetry recovers most ranking signal. Anchored fusion is simpler than stacking, improves mean T1 AUPRC on the same features, and remains competitive with RF in mean while acknowledging non-significant paired tests. With $n{=}6$, intervals are wide; claims emphasize reproducible mean improvements and architecture honesty rather than universal paired dominance.

\subsection{Deep tabular and true GNN baselines}
\label{sec:deep_baselines}
Table~\ref{tab:deep_baselines_v5} compares TabNet and true GraphSAGE with the historical GNN proxy and RF. On T1, TabNet ($0.049$) and true GraphSAGE ($0.015$) trail RF ($0.076$) and ECN-v3 ($0.115$). The historical GNN proxy ($0.034$) must not be read as message-passing performance. On T2, both deep models also trail telem logistic ($0.038$).

\input{tables/tab_deep_baselines_v5}
