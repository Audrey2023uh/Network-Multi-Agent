# Screenshots

Capture after `npm run dev` or Pages deploy:

1. `home.png` — landing + metric cards  
2. `benchmark.png` — Cytoscape topology + inspector  
3. `figures.png` — Plotly ROC / PR  
4. `results.png` — results dashboard  

Place PNG files in this folder. CI does not require them to build.
